import React from 'react';

export default class ReviewButton extends React.Component {
    render() {
        return(
            <button className='btn btn-primary my-3'>Leave a review!</button>
        );
    }
}